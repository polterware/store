# Optional: Supabase connection (dev convenience only).
# If set, the app uses these as fallback when no runtime config exists.
# For production/onboarding flow, leave these empty and configure via the app UI.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY=
